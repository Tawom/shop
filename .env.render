# Environment Variables for Render Deployment
# Copy values to Render Dashboard → Environment Variables

# ========================================
# BACKEND SERVICE ENVIRONMENT VARIABLES
# ========================================

# Node Environment
NODE_ENV=production
PORT=3000

# MongoDB Atlas Connection
# Get this from MongoDB Atlas → Database → Connect → Connect your application
# Replace <password> with your actual database user password
MONGODB_URI=mongodb+srv://ecommerce-user:<password>@cluster0.xxxxx.mongodb.net/ecommerce?retryWrites=true&w=majority

# JWT Secret - GENERATE A STRONG RANDOM STRING
# Use: https://randomkeygen.com/ or run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secure-random-string-change-this-immediately

# Frontend URL - Set after deploying frontend
# Example: https://ecommerce-frontend-xxxx.onrender.com
FRONTEND_URL=https://your-frontend-url.onrender.com

# Email Configuration (Gmail)
# For Gmail App Password: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-gmail-app-password-16-chars
SMTP_FROM=noreply@ecommerce.com

# ========================================
# FRONTEND STATIC SITE ENVIRONMENT VARIABLES
# ========================================

# Backend API URL - Set after deploying backend
# Example: https://ecommerce-backend-xxxx.onrender.com/api
VITE_API_URL=https://your-backend-url.onrender.com/api

# ========================================
# QUICK SETUP CHECKLIST
# ========================================
# 
# □ Setup MongoDB Atlas (free tier)
# □ Create database user and get connection string
# □ Allow network access from anywhere (0.0.0.0/0)
# □ Generate strong JWT_SECRET
# □ Setup Gmail App Password (if using email)
# □ Deploy backend first, copy its URL
# □ Deploy frontend with backend URL
# □ Update backend with frontend URL
# □ Create admin user using Shell
# □ Test all features!
